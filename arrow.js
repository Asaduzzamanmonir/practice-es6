// practice number 3.1=========
const arrow = x => x / 5
console.log(arrow(50));

// practice number 3.2=========

const twoPara = (m, n) => (m + 2) * (n + 2);
const total = twoPara(5, 7)
console.log(total);

// practice number 3.3=====

const threePara = (a, b, c) => a * b * c
console.log(threePara(5, 5, 5));

//Practice number 3.4===========

const multiLine = (a, b) => {
    const sumA = a + 2;
    const sumB = b + 2;
    const result = sumA * sumB;
    return result;
}

console.log(multiLine(5, 6));